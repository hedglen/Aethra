version: "1.0"
linter: qodana-dotnet
withinDocker: false

profile:
  name: qodana.starter

dotnet:
  solution: Aethra.slnx
  configuration: Debug
  platform: x64

bootstrap: dotnet restore Aethra.slnx -p:Platform=x64
